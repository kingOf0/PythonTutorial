# Bu bir yorum satırıdır. Yorum satırları '#' ile başlar. Python, bu satırları görmezden gelir.

# Değişkenler: Bir veriyi hafızada depolamak için değişkenleri kullanırız. Değişken isimlendirmede bir kaç kural
# vardır. Örneğin sayı veya özel işaretler ile başlayamaz veya pythondaki bazı özel anlamlara gelen kelimelerden
# oluşamaz.

# Değişken Tanımlamak:
# 'değişken_ismi = değer' şeklinde kullanılır. Buna 'assign' veya 'assignment' yani 'atama' denir.
# Aşağıda x adında bir değişken oluşturup bunun değerini 5 olarak atadık.
x = 5

# Matematik
# Python'da bir çok cebirsel ifade beklendiği gibi çalışır. Örneğin '+' işareti iki veriyi toplayacaktır.
# Bu toplama sayılarda üstüne ekleme, metinlerde ardına ekleme şeklindedir.
y = x + 2  # y'nin değeri 4 olacaktır. Çünkü x'in değeri 2 olduğunda 2 + 2 = 4 dür.

# Pythonda aşağıdakı arithmetic operatorlar vardır:
# 1. + --> addition yani ekleme ---> örnek: 10+1=11
# 2. - --> substraction yani çıkarma ---> örnek: 10-2=8
# 3. * --> multiplication yani çarpma ---> örnek: 10*3=30
# 4. / --> division yani bölünme ---> örnek: 10/2=5
# 5. // --> kat bölümü ---> örnek: 10:3=3(1) ---> yani 'z=10//3' olduğu zaman 'z' değeri 3'e eşit olacaktır.
# 6. % --> modulus ---> örnek: 11:3=3 (2) --> yani 'z=11%3' olduğu zaman 'z' değeri 2'e eşit olacaktır.
# 7. ** --> üs alma ---> örnek: 2**3=8 4**2=16

# Veri Türleri Her bir verinin bir türü vardır. Örneğin: Tam sayı (integer), ondalıklı sayı (float), doğru / yanlış (boolean), metin Veri (string)
# türleri, veri üzerinde işlem yapmamızı kolaylaştırır. Veriler ile bir işlem yapılırken genellikle aynı tür olmaları
# gerekir. Örneğin karekökünü almak sayılarda kullanılan bir işlev iken bir metnin karekökünü almayı beklemeyiz.

# Veri Türü: Doğru / Yanlış
# 'True' (Doğru) veya 'False' (Yanlış) keywordleri (anahtar kelimeleri) ile ifade edilirler.
# Dikkat! True ve False büyük-küçük harf'e duyarlıdır. Yani true ya da false şeklinde bir kullanım yoktur.
# Aşağıda 'z' adında bir değişken oluşturduk ve True yani Doğru değerine atadık.
z = True


